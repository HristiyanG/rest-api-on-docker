{
  "name": "restapi-mongo-docker",
  "version": "1.0.0",
  "description": "REST API with MongoDB on docker",
  "main": "index.js",
  "scripts": {
    "start": "docker-compose -f docker/docker-compose.yml up -d",
    "build": "docker-compose -f docker/docker-compose.yml build"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "mongodb": "^3.1.8"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/HristiyanG/rest-api-on-docker.git"
  }
}
